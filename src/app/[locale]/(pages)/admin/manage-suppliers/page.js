'use client';

import { Grid, Typography } from '@mui/material';

export default function ManageSuppliersPage() {
  return (
    <Grid size="12" padding={'10px'}>
      <Typography>Manage suppliers</Typography>
    </Grid>
  );
}
